@AGENTS.md

# OPERATING SYSTEM
This project runs under the IMS Build System (build-time layer beneath the global Powerhouse loop; stricter rule wins on conflict).
Canonical files: `C:\Users\Admin\.claude\build-system\00-MASTER.md` — read before any build work; load the rest per its routing table.
Current phase and state: `STATE.json` (repo root). Gate reports: `docs/gates/`. Spec of record: `SITE_PLAN.md`.
