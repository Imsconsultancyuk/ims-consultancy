// Content validation entrypoint, run via `npm run validate:content` and
// chained ahead of `next build` (IMS-003). Placeholder until IMS-011 wires
// the real Zod schema + registry — replaced there, not extended here.
console.log("validate:content — no industries registered yet (IMS-011 pending)");
